# Release Plan

Remaining work before a public `v0.1.0-alpha` release tag:

- Cross-platform hardening
- GitHub Actions setup
- Release binaries
- VSIX regeneration
- README final pass
- Tag `v0.1.0-alpha`

## Notes

- The language and runtime are already usable for alpha examples
- The main remaining work is packaging, release hygiene, and platform verification rather than large new language features
